import { For } from "solid-js";

export default function ThreatModelProperties(props) {
    const element = props.element;

    return (
        <div>
            {/* <For each={element()}>{(e, i) => <div>{e}</div>}</For> */}
        </div>);
}